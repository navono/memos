services:
  memos:
    # Use the locally built image (make docker-build) instead of the upstream one.
    image: navono/memos:personal
    pull_policy: never
    container_name: memos
    volumes:
      - /home/ubuntu/data/memos:/var/opt/memos
    ports:
      - 5230:5230
